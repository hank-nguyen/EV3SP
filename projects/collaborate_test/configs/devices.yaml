# Collaborate Test Device Configuration
# Devices are hardware platforms, NOT projects
# Projects (like "puppy") are built on top of devices

devices:
  ev3:
    platform: ev3
    # transport: micropython  # Default! Uncomment for explicit setting
    # transport: ssh          # Legacy SSH mode (slower, 30-50ms)
    host: 192.168.68.111
    wifi_port: 9000           # For MicroPython TCP
    # SSH settings (only used if transport: ssh)
    user: robot
    password: maker
    sudo_password: maker

  spike:
    platform: spike_prime
    name: Avatar Karo
    address: E1BDF5C6-C666-4E77-A7E8-458FC0A9F809
