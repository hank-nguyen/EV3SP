# EV3 Puppy Remote Connection Configuration
# Default: MicroPython (USB Serial / WiFi TCP) - fastest!

mode: remote
transport: micropython  # "micropython" (default, 1-15ms) or "ssh" (legacy, 30-50ms)

# MicroPython settings
host: 192.168.68.114
wifi_port: 9000

# Legacy SSH settings (only used if transport: ssh)
user: robot
password: maker
sudo_password: maker
ssh_port: 22

work_dir: /home/robot/ev3
